{
  "watch": ["server/src", "server/config"],
  "ext": "ts,json",
  "ignore": ["**/*.spec.ts"],
  "exec": "ts-node --project tsconfig.server.json server/src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}